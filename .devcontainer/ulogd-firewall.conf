# ulogd2 config for devcontainer firewall logging
# Receives NFLOG group 1 packets and writes to /var/log/firewall.log
#
# iptables LOG target doesn't work in Docker containers (writes to host
# kernel log which is inaccessible). NFLOG + ulogd2 works via netlink
# in userspace.

[global]
logfile="/var/log/ulogd.log"

# Plugin libraries (Debian x86_64)
plugin="/usr/lib/x86_64-linux-gnu/ulogd/ulogd_inppkt_NFLOG.so"
plugin="/usr/lib/x86_64-linux-gnu/ulogd/ulogd_raw2packet_BASE.so"
plugin="/usr/lib/x86_64-linux-gnu/ulogd/ulogd_filter_IFINDEX.so"
plugin="/usr/lib/x86_64-linux-gnu/ulogd/ulogd_filter_IP2STR.so"
plugin="/usr/lib/x86_64-linux-gnu/ulogd/ulogd_filter_PRINTPKT.so"
plugin="/usr/lib/x86_64-linux-gnu/ulogd/ulogd_output_LOGEMU.so"

stack=log1:NFLOG,base1:BASE,ifi1:IFINDEX,ip2str1:IP2STR,print1:PRINTPKT,emu1:LOGEMU

[log1]
group=1

[base1]

[ifi1]

[ip2str1]

[print1]

[emu1]
file="/var/log/firewall.log"
sync=1
