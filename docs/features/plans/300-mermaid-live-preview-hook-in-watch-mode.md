# Implementation Plan: Mermaid Live-Preview Hook in Watch Mode

## Overview
Extend `watch` and `sync` commands to export Mermaid C4 diagrams automatically to `architecture.md` on every model change. This enables instant architecture preview in GitHub/GitLab browsers without draw.io.

## Phase 1: Foundation — Markdown Wrapper & C4 Level Detection

### 1.1 Create `internal/diagram/markdown.go`
- New file: `markdown.go` with function `WrapDiagramsInMarkdown(views []string, diagrams map[string]string) string`
- Input: map of view keys → Mermaid diagram code
- Output: Single Markdown file with:
  - Header: "# Architecture Diagrams"
  - Metadata comment: "Auto-generated by bausteinsicht — do not edit manually"
  - Timestamp: "Last updated: 2026-03-18T14:32:00Z"
  - One Markdown section per view with Mermaid code block
- Preserve view order
- Handle empty views gracefully

### 1.2 Extend `internal/diagram/mermaid_c4.go` — C4 Level Detection
- Add function `DetectC4Level(view *model.View, scope string, elements map[string]*model.Element) string`
- Returns: `"C4Context"`, `"C4Container"`, `"C4Component"`, or `"C4Dynamic"`
- Logic:
  - No scope + top-level elements → `C4Context`
  - Scope with `kind == "system"` → `C4Container`
  - Scope with `kind == "container"` → `C4Component`
  - Else → `C4Context` (default)
- Unit tests: Test each level type + edge cases

### 1.3 Extend Mermaid Export to Handle Full Model
- Modify existing Mermaid exporter to handle all views (not just structural views)
- Add support for rendering full model scope diagrams
- Ensure C4 level is correctly used in diagram header

**Deliverable:** `markdown.go` + updated `mermaid_c4.go` with passing unit tests

---

## Phase 2: CLI Flags — Add `--mermaid` & `--mermaid-output` to Sync/Watch

### 2.1 Update `cmd/bausteinsicht/sync.go`
- Add flags:
  - `--mermaid` (bool, default false): Enable Mermaid export after sync
  - `--mermaid-output` (string, default "architecture.md"): Output file path
- In `runSync()` after successful draw.io sync:
  - If `--mermaid` is set, call Mermaid export
  - Write diagrams to specified output file
  - Log: "✅ Exporting Mermaid... architecture.md (3 views)"
- Error handling: If Mermaid export fails, log warning but don't fail sync

### 2.2 Update `cmd/bausteinsicht/watch.go`
- Add same flags as `sync.go`
- Pass flags to watcher loop (see Phase 3)

### 2.3 Unit Tests
- Test: `sync --mermaid --mermaid-output custom.md` creates the file
- Test: `sync --mermaid` with default output creates `architecture.md`
- Test: `sync` without `--mermaid` flag does NOT create Mermaid file

**Deliverable:** Updated `sync.go` and `watch.go` with flags + unit tests

---

## Phase 3: Watch Mode Integration — Post-Sync Hook

### 3.1 Extend `internal/watcher/watcher.go`
- Add parameters to `Run()`:
  - `enableMermaid bool`
  - `mermaidOutput string`
- After each successful `sync.Run()` in the watch loop:
  - If `enableMermaid` is true AND sync succeeded:
    - Export all views to Mermaid diagrams
    - Wrap in Markdown via `markdown.WrapDiagramsInMarkdown()`
    - Write to `mermaidOutput` file
    - Log success: "✅ Exporting Mermaid... architecture.md (3 views)"
  - If sync failed, skip Mermaid export (don't update file)

### 3.2 Update Watch Command Handler
- Pass `--mermaid` and `--mermaid-output` flags to watcher
- Test output:
  ```
  [14:32:01] Change detected: architecture.jsonc
  [14:32:01] Syncing to draw.io...    ✅ (12 elements, 8 relationships)
  [14:32:01] Exporting Mermaid...     ✅ architecture.md (3 views)
  ```

### 3.3 Unit Tests
- Test: Mermaid file written after successful sync in watch mode
- Test: Mermaid file NOT updated if sync fails
- Test: Multiple watches don't interfere (isolation)

**Deliverable:** Updated `watcher.go` + watch command integration + tests

---

## Phase 4: Markdown Output & Edge Cases

### 4.1 Test Coverage
- Test: Empty model (0 views) → empty Markdown with header
- Test: Single view → single Markdown section
- Test: Multiple views → multiple sections in order
- Test: View with special characters in name
- Test: Very large model (100+ elements)
- Test: Relationship rendering in C4 diagrams

### 4.2 Documentation
- Update help text for `--mermaid` and `--mermaid-output` flags
- Add section to CONTRIBUTING.md or README about Mermaid export
- Document C4 level detection logic

### 4.3 Integration Test
- End-to-end: Load example model, run `sync --mermaid`, verify `architecture.md` contains valid Mermaid syntax
- Verify markdown is valid GitHub flavor

**Deliverable:** Full test coverage + passing integration tests

---

## Success Criteria
1. ✅ `sync --mermaid` creates valid Markdown + Mermaid diagrams
2. ✅ `watch --mermaid` exports after each successful sync
3. ✅ C4 level auto-detected correctly per view scope
4. ✅ All unit tests passing (lint, build, race detector)
5. ✅ Mermaid output renders in GitHub web UI
6. ⏳ CI/CD pipeline green (all platforms) — in progress

## Implementation Status
- **Phase 1 (Foundation)**: ✅ COMPLETE
  - `markdown.go` created with `WrapDiagramsInMarkdown()`
  - C4 level detection integrated via existing `detectLevel()`
  - `ExportAllViewsToMermaid()` function implemented
  
- **Phase 2 (CLI Flags)**: ✅ COMPLETE
  - `--mermaid` and `--mermaid-output` flags added to `sync` command
  - `--mermaid` and `--mermaid-output` flags added to `watch` command
  - Mermaid export integrated into sync workflow
  
- **Phase 3 (Watch Integration)**: ✅ COMPLETE
  - Post-sync Mermaid export implemented in `doSync()` callback
  - Only exports when sync succeeds (skips on failure)
  
- **Phase 4 (Testing)**: ✅ COMPLETE
  - Unit tests for Markdown wrapper (4 tests)
  - Unit tests for multi-view export (2 tests)
  - Integration tests for sync command (3 tests)
  - E2E verification: All tests passing

## PR Status
- Branch: `300-mermaid-live-preview-hook-in-watch-mode`
- PR: #349
- CI: Running (Go CI queued/in progress)

---

## Known Constraints
- Mermaid syntax is limited to text format (no styling within C4 blocks)
- C4 relationships use `Rel()` syntax; may need label truncation for very long labels
- Watch mode requires successful sync before Mermaid export runs

---

## Timeline Estimate
- Phase 1: 2-3 hours
- Phase 2: 1-2 hours
- Phase 3: 1-2 hours
- Phase 4: 1-2 hours
- **Total: 5-9 hours** (including testing & iteration)
